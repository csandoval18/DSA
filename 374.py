class Solution(object):
  def guessNumber(self, n):
    self.binarySearchHelper(1, n)
  
    def binarySearch(l, r):
      # base case 
      if l <= r:
        m = (l + r) // 2
        
        if m == x:
          return mid
      
      

        
      return
      